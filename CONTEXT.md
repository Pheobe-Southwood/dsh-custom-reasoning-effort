# dsh-custom-reasoning-effort 上下文

DSH 的推理等级补全插件：让通过自定义提供方接入的模型，与目录提供方的模型一样出现在输入框的推理等级选择器里。本文件是项目领域语言的术语表。

## 提供方与路由

**提供方路由（Provider route）**：
一个可寻址的模型提供方条目，以自己的路由键与其它路由区分；接入信息、模型清单与其全部能力事实都只属于它所在的那条路由。同一个模型 ID 挂在两条路由下就是两条互不相干的路由。
_Avoid_: 提供方、供应商、通道

**目录提供方（Catalog provider）**：
其模型能力由随 harness 一同安装的内置目录描述的提供方路由：模型 ID 一落入目录，就自带上下文窗口、模态与推理能力等事实。
_Avoid_: 内置提供方、官方提供方、预置提供方

**自定义提供方（Custom provider）**：
由用户自行添加、在内置目录里没有对应条目的提供方路由。它只带用户填写的接入信息与模型 ID 清单，因此其模型默认没有任何能力事实。
_Avoid_: 第三方提供方、私有提供方、外部提供方

**不可解析路由（Unresolvable route）**：
接入信息无法被解析成可用配置、因而完全无法写入的提供方路由。它不会被强行写入：为了不让整个回合唯一的那次写入失败，规范化会跳过它，让其余路由照常补齐；路由修好之后，下一轮自然重新纳入。
_Avoid_: 坏路由、无效提供方、解析失败

## 目录与能力

**模型目录（Model catalog）**：
harness 为每条提供方路由建立并持续刷新的模型能力视图，也是模型选择界面唯一的读取来源：目录里没有的事实，界面上就不会出现。
_Avoid_: 模型列表、模型表、注册表

**目录孪生（Catalog twin）**：
内置目录里与某个模型 ID 相同的条目。能力事实按路由分开存放，所以同一个 ID 可能同时出现在几条目录路由上；只有这些同名条目彼此一致时，它才算一个可用的事实来源，否则这个 ID 在不同地方指的不是同一个模型。
_Avoid_: 同名模型、内置版本、对应条目

**推理等级（Reasoning effort）**：
一次请求可以选择的思考强度档位。它由模型的能力事实决定：声明了推理能力的模型才有等级可选，没有声明的模型在选择器里根本不出现。
_Avoid_: 推理强度、思考等级、思考深度

**思维档位（Thinking level）**：
推理等级所使用的标准级别词汇，从一个都不思考直到最强档。档位是词汇，能力是「这个模型有没有档位可选」；二者不是一回事。同一个档位在不同提供方那里对应不同的线上取值。
_Avoid_: 强度档、level、档位表

**标准档位集（Standard set）**：
模型缺少可用事实来源时使用的完整档位集合，从 off 起直到最高档，每个档位以自己的名字作为线上取值。它是「不知道」时的兜底，不是对该模型能力的断言。
_Avoid_: 默认档位、全档位、兜底值

## 设置层

**用户设置层（User settings layer）**：
用户自己持久化的那层设置，与随 harness 或预设下发的组合层相对。规范化只读这一层，也只往这一层写。
_Avoid_: 配置文件、用户配置、settings

**组合层（Composition layer）**：
随 harness 或预设一起下发、用户不直接拥有的基线配置。同一条提供方路由可能只出现在其中一层里；只出现在组合层里的路由不归规范化管。
_Avoid_: 默认配置、基线层、内置配置

## 字段与语义

**reasoningEfforts 字段**：
模型条目上的推理能力声明，逐模型生效。给出档位字典表示该模型提供这些档位，取值 false 表示该模型不提供推理；字段缺失则意味着这份能力应当从别处继承——而自定义提供方没有可继承的来源。
_Avoid_: 推理配置、reasoning 字段、effort 字段

**退出开关（Opt-out）**：
模型条目上显式写下的「此模型不提供推理」的声明，它的形式只有显式写下的 false。它表达的是判断而不是缺失，因此必须与「没有填写」区别对待，并且永远优先于补全；一份档位字典不是判断，而是派生状态。
_Avoid_: 关闭项、禁用标记、例外

**派生状态（Derived state）**：
由当前事实重新算出来的字段值，与用户写下的判断相对。派生状态在每次启动与每次设置变更时都会被重算，重算结果与现值相同则不产生写入。
_Avoid_: 缓存值、自动填充、默认值

**规范化（Normalization）**：
在不改变用户的接入信息与模型选择的前提下，把缺失的能力事实补齐为目录本来就会有的形状。规范化只补空缺与可继承的事实，不发明能力；它唯一会覆盖的是上一次算出来的派生状态，用户写下的判断永远保留。
_Avoid_: 迁移、修复、补丁、清洗
